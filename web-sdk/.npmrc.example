# 复制为 .npmrc 后使用（.npmrc 已被 .gitignore，勿提交真实 token）。
# 把 @yuanmai 换成你们的私有 scope，把域名换成你们的私有 registry。
# CI / 本地通过环境变量 NPM_TOKEN 注入鉴权，不要把明文 token 写进文件。

@yuanmai:registry=https://npm.cnb.cool/fucksky/yuanmai/registry/
//npm.cnb.cool/fucksky/yuanmai/registry/:_authToken=${NPM_TOKEN}
always-auth=true
